// assets/js/admin.js
document.addEventListener('DOMContentLoaded', function() {
    // Empty logic file. We kept logic inside the PHP view mostly to avoid external dependencies.
    // If you need Chart.js logic, add it here and include the library in dashboard.php.
});